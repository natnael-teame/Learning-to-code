import time, datetime

try:
    while True:
        s = datetime.datetime.now()
        ss = s.strftime("%H:%M:%S")
    
        print("time: ", ss, end="\r", flush=True)
        
        time.sleep(1)
except KeyboardInterrupt:
    exit()
